"""
Shared utilities for Phase 2 subphases (top-level ``phase_2_*`` folders).

- ``config`` — workspace root, JSON config, environment overrides
- ``paths`` — artifact directory layout (raw / clean / metadata / extracted)
- ``registry_bridge`` — Phase 1 ``source_registry`` + allowlist validation
- ``logging_setup`` — stderr logging with optional PII redaction
"""
