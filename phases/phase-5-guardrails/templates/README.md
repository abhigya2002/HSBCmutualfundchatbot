# Composer templates (Phase 5.3+)

| File | Purpose |
|------|---------|
| `refusal.templates.json` | Fixed refusal copy for Phase 5.3 |
| `factual.templates.json` | Extractive factual sentence templates for Phase 5.4 |

Factual templates will be extended in later phases if LLM composition is enabled. Defaults also pinned in `config/guardrails.defaults.json`.
