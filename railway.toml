[build]
builder = "RAILPACK"

[deploy]
startCommand = "cd phases/phase-6-api-ui && python -m phase_6_2.run_server --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
